# Copy to .env locally. Never paste or commit secrets.
LLM_PROVIDER=openai
# Supported: openai, anthropic, openai_compatible
LLM_MODEL=
LLM_API_KEY=
# Optional OpenAI-compatible API endpoint
LLM_BASE_URL=
# Optional. Leave blank for automatic full-database/demo-database selection.
DUCKDB_PATH=
