# Implementation and verification status

Updated: 2026-09-22 (Asia/Singapore)

本文件是事实清单：“已实现”表示代码存在，“已验证”表示在当前本地检出中实际执行过。缺失的外部条件不会用模拟结果替代。

## 已实现并验证

| 范围 | 证据 |
|---|---|
| 真实公开全量数据 | Inside Airbnb Sydney `2026-06-16`；本地全量库含 20,573 房源、7,509,145 日历行、801,398 评论和 10 个 POI。原始文件 URL、大小、SHA-256 在本地 `data/raw/manifest.json`，审计摘要在 `docs/data_audit.json`。 |
| 可部署真实演示数据 | `scripts/build_demo_database.py` 从全量库确定性抽样，生成仓库内 `data/staylens_demo.duckdb`；metadata 明确标记 `real_public_sample`，界面和结果均提示非全量。 |
| 数据来源门禁 | 推荐流程仅接受 `real_public_snapshot` 或 `real_public_sample`；未知来源数据库被拒绝。 |
| 硬约束与日期诚信 | 人数、晚数、预算、房型、区域、最短/最长入住和距离使用参数化逻辑；未知地点失败关闭。本快照缺少每日价格，因此指定日期预算查询会阻断，不以基础价冒充。 |
| 排名与解释 | 独立市场价格参照、贝叶斯评分收缩、haversine 距离、稳定并列规则、可归一化权重和结果追溯。 |
| 评论分析 | 八类主题、四类情感、原文子串 evidence 验证、批次验证和版本化缓存；缺失分类给中性分并告警。 |
| Text-to-SQL 安全 | 单 SELECT、AST 校验、表/列/函数白名单、禁用外部访问、只读 DuckDB、超时中断和外层 200 行限制。 |
| UI | 面向用户的住宿筛选、自然语言输入、市场洞察、地图、推荐理由、必要的数据限制说明与 CSV 导出；SQL、原始 JSON、模型运行模式等技术细节不在前台展示。 |
| 自动化验证 | 全量库与演示库模式均为 44/44 tests passed；确定性端到端 10/10、SQL 安全边界 14/14；演示库 Streamlit AppTest 三个页面加载、0 异常。详见 `evaluation/latest_report.json`。 |

## 真实模型评测

| 套件 | 一次性结果 | 边界 |
|---|---|---|
| Intent final-v2 | 95/100 整题正确；342/352 字段正确（97.16%） | 五个失败；冻结后未依据结果调参或重跑。 |
| Text-to-SQL final-v2 | 48/50 状态/执行正确（96%）；41/50 输出契约正确（82%）；39/50 语义结果正确（78%） | 两个错误拒绝、七个额外契约失败、两个语义不一致/不完整。 |
| Review reference-v1 | 严格主题+情感 P/R/F1 = 79.35%/52.90%/63.48%；原文证据合规率 89.13% | 55 条真实评论、138 个 Codex 辅助整理标签；不是人工金标准，四个 review ID 在一个批次被漏掉。 |

final-v2 共记录 167 次 API 调用、252,667 tokens；按评测日官方 `gpt-4o-mini` 价格估算为 US$0.04623。详细失败案例、提示词哈希和逐题输出保留在 `evaluation/` 最终资产中。

## 现在可以和不可以宣称的内容

可以宣称：使用真实公开数据、具有可追溯的确定性分析层、完成真实 API 冻结评测、代码和演示数据已整理为 GitHub 作品集结构。

可以宣称代码已发布为公开 GitHub 仓库：`https://github.com/luluyang586/staylens-lite`。不可宣称：实时库存/价格、真实入住率或订单、因果增益、人工金标准评论准确率、生产级稳定性，或“应用已经在线部署”。本次发布只完成代码托管，尚未创建 Streamlit 线上服务。

## 发布前剩余外部步骤

1. 在 Streamlit Community Cloud 选择 `app.py` 部署；LLM 密钥只放平台 Secrets。
2. 在安装 Docker 的机器上实际构建镜像并记录结果。本机没有 Docker，因此 Dockerfile 只能标记为“已提供、未实机验证”。

## 后续质量提升（不阻塞作品集发布）

1. 由两名人工标注者独立标注并仲裁评论参考集，再报告 human-ground-truth 指标。
2. 将后续提示词优化放入新的开发集，并新建 final-v3；不得继续调优后复用 final-v2 作为未见测试。
3. 增加真实云部署后的启动、冷启动时间与资源占用记录。
